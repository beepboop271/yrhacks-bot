{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Config",
  "description": "Configuration options for the bot that do not need to be private",
  "type": "object",
  "properties": {
    "prefix": {
      "type": "string",
      "description": "The command prefix for the bot, such as '!'",
      "minLength": 1
    },
    "channelFile": {
      "type": "string",
      "description": "The path to the server channel config json file",
      "minLength": 1
    },
    "roleFile": {
      "type": "string",
      "description": "The path to the server role config json file",
      "minLength": 1
    },
    "wordlistFile": {
      "type": "string",
      "description": "The path to the banned word list json file",
      "minLength": 1
    }
  },
  "required": [
    "prefix"
  ]
}
